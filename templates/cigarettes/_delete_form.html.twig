<form method="post" action="{{ path('app_cigarettes_delete', {'id': cigarette.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cigarette.id) }}">
    <button class="btn">Delete</button>
</form>
