{% extends 'base.html.twig' %}

{% block title %}Drink{% endblock %}

{% block body %}
    <h1>Drink</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ drink.id }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ drink.Type }}</td>
            </tr>
            <tr>
                <th>Alcohol</th>
                <td>{{ drink.Alcohol ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>Hot</th>
                <td>{{ drink.Hot ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_drink_index') }}">back to list</a>

    <a href="{{ path('app_drink_edit', {'id': drink.id}) }}">edit</a>

    {{ include('drink/_delete_form.html.twig') }}
{% endblock %}
